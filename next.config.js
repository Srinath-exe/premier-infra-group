/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: '/premier-infra-group',
    // assetPrefix: '/premier-infra-group/',
    // output: 'export',
};

module.exports = nextConfig;
